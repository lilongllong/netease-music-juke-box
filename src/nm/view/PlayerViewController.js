import View from "../../nju/view/ViewController";

export default class PlayerController extends ViewController
{
    createView(options)
    {
        return new PlayerView();
    }

    initView()
    {
        this.
    }
}
